# Provenance scope

The generated candidate is produced only by `generator/generate_candidate.py` from the
indexed reference source. The captured development evidence is bound through
`evidence/index.json`. This L4 study does not claim L5 immutable provenance or release
attestation.
